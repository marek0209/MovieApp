import React from "react";
import { storiesOf } from "@storybook/react";
import LoadingSpinner from "./LoadingSpinner";

storiesOf("Components/LoadingSpinner", module).add("Default", () => (
  <LoadingSpinner />
));
